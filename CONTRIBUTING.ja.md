# コントリビューションガイド

[English](CONTRIBUTING.md)

`jellycli`の改善に関心をお寄せいただき、ありがとうございます。

## 開発環境

Go 1.25以降が必要です。再生機能を開発する場合は、`mpv`と、利用権限のあるJellyfinサーバーも必要です。

```sh
git clone https://github.com/minittupoyo/jellycli.git
cd jellycli
go test ./...
go build ./cmd/jellycli
```

変更を提出する前に、次を実行してください。

```sh
gofmt -w .
go vet ./...
go test ./...
```

## 設計方針

コマンド解析と画面表示を、Jellyfin APIアクセスおよび再生制御から分離してください。Jellyfinの新しい操作はクライアント層へ、ターミナルの状態やキー操作はTUI層へ追加します。各パッケージの役割は[アーキテクチャ](docs/architecture.ja.md)を参照してください。

変更は小さく目的を絞り、可能な限りテストを追加してください。アクセストークン、サーバーURL、個人のメディア名などの非公開情報を、ソース、テストデータ、ログ、スクリーンショット、Issue、Pull Requestに含めないでください。

## Pull Request

Pull Requestでは次を確認してください。

- ユーザーから見た変更内容と目的を説明する
- 新しいロジックや不具合修正に、可能な限りテストを追加する
- コマンド、操作、設定、対応環境が変わる場合はドキュメントを更新する
- フォーマット、vet、テスト、GitHub Actionsのビルドを通す
- Jellyfinや`mpv`で手動確認した内容を記載する

再生機能を変更した場合は、可能であれば開始、一時停止と再開、シーク、最後までの再生、途中終了を確認してください。Jellyfin側の進捗と視聴済み状態が適切に更新されることも確認します。

## リリース

メンテナーは変更履歴を更新してリリース時点をコミットし、セマンティックバージョニングに従ったタグをpushします。

```sh
git tag -a vX.Y.Z -m "vX.Y.Z"
git push origin main vX.Y.Z
```

リリースワークフローがアーカイブとチェックサムをビルドし、GitHub Releaseを作成します。

## ライセンスの状態

現在、このリポジトリにはソフトウェアライセンスがありません。メンテナーがライセンスを選択して追加するまで、オープンソースライセンスに基づくコントリビューションは受け付けていません。大きな変更を行う前にメンテナーへ相談してください。
